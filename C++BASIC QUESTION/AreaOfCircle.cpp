#include<iostream>
using namespace std;
int main()
{
    int radius,area;
    cout<<"enter the radius of circle : ";
    cin>>radius;
    area=3.14*radius*radius;
    cout<<"area of circle : "<<area;

    return 0;
}